app:
  name: "gorduchinha"
  debug: true

db:
  host:
  port:
  user:
  pass:
  name:

cache:
  host:
  port:
  user:
  pass:
  db: 0
  prefix: "gorduchinha"
  default-expiration: "24h"

server:
  port: 8080
  prefix: "/api/v1"
  auth:
    clients-urls:
      - "*"
  rate-limit:
    period: "1s"
    limit: 10

log:
  log-to-file: false

http-client:
  timeout: "60s"
